# Configuração do Atlas — copie para .env e preencha com seus valores
# NUNCA commite .env no repositório (ver .gitignore)

# Telegram
# Token do seu bot do Telegram (criar em https://t.me/botfather)
TELEGRAM_TOKEN=seu_token_aqui

# Seu ID do Telegram (envie /start para @userinfobot para descobrir)
ATLAS_ALLOWED_USER_ID=seu_id_aqui

# Banco de dados (opcional)
# ATLAS_DB_PATH=atlas.sqlite

# Diretório de rotinas (opcional)
# ATLAS_ROUTINES_DIR=routines

# Timeout de polling (opcional, em segundos)
# ATLAS_POLL_TIMEOUT=30

# Multiusuário / segurança (ADR-0027)
# Token de admin (loopback já é admin em dev); chave mestra do cofre
# ATLAS_API_TOKEN=
# ATLAS_SECRET_KEY=
# Código de auto-registro: se setado, habilita "criar conta" na tela de login
# (usuário escolhe user+senha; papel sempre member). Vazio = auto-registro desabilitado.
# ATLAS_SIGNUP_CODE=
# Endurecimento do Agente modo code (ADR-0028) e sessões persistidas
# ATLAS_AGENT_MAX_CONCURRENT=3
# ATLAS_SESSIONS_PATH=
